{{ header }} {{ column_left }}

<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <h1>{{ lang_heading_title }}</h1>
        </div>
    </div>
    <div class="container-fluid">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">{{ lang_heading_title }}</h3>
            </div>

            <div class="panel-body">
                <form class="" action="{{ action }}" method="post">
                    <div class="">
                        <label for="telegram_chat_ids">ID через пробел</label>
                    </div>
                    <div class="">
                        <textarea id="telegram_chat_ids" name="telegram_chat_ids" rows="8" cols="80">{{ setting_telegram.fe_telegram_chat_ids }}</textarea>
                    </div>

                    <div class="">
                        <button type="submit" name="submit" class="btn btn-primary">Отправить</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

{{ footer }}
